{
  "devDependencies": {
    "@types/jest": "^29.1.1",
    "@types/node": "^20.4.5",
    "@types/sqlite3": "^3.1.8",
    "gts": "^5.0.0",
    "ts-jest": "^29.1.1",
    "typescript": "~5.1.6"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "test": "jest"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "sqlite3": "^5.1.6"
  }
}
